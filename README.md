# M16-CPU-Emulator
This is an experimental version of my M16 CPU, with it's custom ISA. Made by Mohib Ahmad.
The emulator takes instructions in it's "program" variable near at the end
programs are written as a tuple:
program = [
  ("opcode", "operand", "operand")
]
